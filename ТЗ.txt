Описать произвольный тип со свойствами разных типов (строка, число, дата, bool)генерировать 100 объектов с произвольными значениямисделать выборки с применением операторов
where
order by
group by
any, all
sum, min, max